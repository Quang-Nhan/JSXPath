import { nodeScenarios } from './NodeScenarios';
import { Scenario } from '../JSXInterfaces';

describe('nodeScenarios()', () => {
  let currentIndex: number, parsedPath: string, chars: string, tests: Array<Scenario>;

  describe('Is node and node is end of path.', () => {
    
  });
});