import JSXMain from './JSXMain';

describe('JSXMain', () => {
  let main;
  
  describe('Operators', () => {

    it('+', () => {
      const path = '1+2';
      // main = new JSXMain(path, )
    });
  });
})