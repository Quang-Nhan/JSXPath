var JSXPloder = require("./JSXPloder");

describe("JSXPloder", () => {
	beforeEach(() => {

	});


	describe("explode()", () => {

	});

	describe("_explode()", () => {
	});



	describe("_updateChildren()", () => {

	});

	describe("_explodeObject()", () => {

	});

	describe("_explodeArray()", () => {

	});

	describe("_setDefaultProperties()", () => {

	});

	describe("_createProperty()", () => {

	});
});
